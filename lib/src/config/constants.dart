import 'package:flutter/material.dart';

const primaryColor = Color(0xFF881A5A);
const secondryColor = Color(0xFFF7F7F7);
final svgIconColor = Colors.grey.shade500;

const defualtDuration = Duration(milliseconds: 2250);

const defultShadow = BoxShadow(
  color: Colors.black12,
  blurRadius: 2,
  offset: Offset(-5, 5),
);
